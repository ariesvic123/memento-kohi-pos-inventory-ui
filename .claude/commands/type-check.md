# /project:type-check

Run TypeScript type checking on the entire project and report errors.

## Instructions
1. Run: `npx tsc --noEmit`
2. If there are errors — list them grouped by file, with the exact line numbers
3. For each error, suggest the fix (one line, direct — no essay)
4. If zero errors — say "Clean. No type errors." Nothing more.
